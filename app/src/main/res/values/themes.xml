<resources>
    <style name="Theme.PSM" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="CustomDatePickerDialog" parent="ThemeOverlay.MaterialComponents.Dialog">
        <!-- Customize the background color -->
        <item name="android:windowBackground">@color/white</item>
        <!-- Customize the text color -->
        <item name="android:textColorPrimary">@color/black</item>
        <!-- Customize the accent color -->
        <item name="colorAccent">@color/dark_blue</item>
    </style>

    <style name="CustomRatingBar" parent="@style/Widget.AppCompat.RatingBar.Small">
        <item name="android:progressTint">@color/dark_blue</item>
        <item name="android:progressBackgroundTint">#00000000</item>
    </style>


</resources>
